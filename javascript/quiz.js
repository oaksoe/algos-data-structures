// 1. Given a const array ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'], a day 'Wed' and integer N, return the day which is N days after the input day.

// 2. Given an integer N, return the smallest integer, bigger than N, where N should not have any consecutive 2 same digits.

// 3. Given an array A with each element's value is an integer, find 2 minimum values that can break the array into 3 sub-arrays

// 4. Write a ByteArray class that can hold any size up to 128000 items, assuming a JS list can hold only 64000 items.